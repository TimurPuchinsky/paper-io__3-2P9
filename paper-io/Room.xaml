<Window x:Class="paper_io.Room"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:paper_io"
        mc:Ignorable="d"
        Title="Room" Height="511.5" Width="924.5">
    <Grid>
        <ScrollViewer x:Name="CanvasViewer"   HorizontalScrollBarVisibility="Hidden"  VerticalScrollBarVisibility="Hidden" Margin="205,54,153,53" Background="#FFA3ABF9">
            <Canvas x:Name="Canvas"  IsHitTestVisible="False" Height="225" Width="231" Background="#FFFDEBEB">
                <Rectangle x:Name="Player" Fill="Black" Canvas.Left="100" Canvas.Top="86" Height="40" Width="36" />
            </Canvas>

        </ScrollViewer>
        <!--<ScrollViewer x:Name="CanvasViewer" HorizontalScrollBarVisibility="Hidden"  VerticalScrollBarVisibility="Hidden" Margin="290,146,296,145" Background="#FFF5DADA">
            <Canvas x:Name="Player" IsHitTestVisible="False" Background="Black" Height="39" Width="36"/>
            -->
        <!--<Rectangle x:Name="Player" Fill="Black" Canvas.Left="90" Canvas.Top="86" Height="50" Width="50" />-->
        <!--
        </ScrollViewer>-->
        <Label Content="Label" HorizontalAlignment="Left" Margin="392,266,0,0" VerticalAlignment="Top"/>
    </Grid>
</Window>
